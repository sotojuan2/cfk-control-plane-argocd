apiVersion: platform.confluent.io/v1beta1
kind: KafkaTopic
metadata:
  name: demo-topic-1
  namespace: confluent
spec:
  replicas: 3
  partitionCount: 4
  configs:
    message.timestamp.type: "LogAppendTime"
  kafkaRest:
    authentication:
      type: basic
      basic:
        secretRef: "cloud-credentials"
    endpoint: https://pkc-7xoy1.eu-central-1.aws.confluent.cloud:443
    kafkaClusterID: "lkc-ong22x"
